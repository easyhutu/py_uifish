import logging
from logzero import logger
from uifish.settings import cache

# logging.basicConfig(level=cache.get('log_level'),
#                     format='%(asctime)s : %(levelname)s : %(filename)s:%(lineno)s - %(message)s')
# logger = logging.getLogger(__name__)
